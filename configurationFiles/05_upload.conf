server {
    listen 8081;
    server_name localhost;

    root ./www/site1;
    index index.html;

    location / {
        allow_methods GET;
    }

    location /upload {
        allow_methods POST;
        upload_path ./www/site1/uploads;
    }
}
